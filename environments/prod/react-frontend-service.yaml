apiVersion: v1
kind: Service
metadata:
  name: react-frontend-service
  namespace: production
spec:
  type: NodePort
  selector:
    app: react-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080  # Accessible via :30080